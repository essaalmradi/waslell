# waslell

Railway deployment source for the Waslek Laravel platform.

The historical application bundle is used as the immutable base. `architecture-overlay.tar.gz` contains the current architecture/auth/request-lifecycle refactor and is extracted on top during build. See `docs/00-START-HERE.md` inside the running application.
